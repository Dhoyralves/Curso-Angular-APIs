import { NultilicaPorPipe } from './nultilica-por.pipe';

describe('NultilicaPorPipe', () => {
  it('create an instance', () => {
    const pipe = new NultilicaPorPipe();
    expect(pipe).toBeTruthy();
  });
});
